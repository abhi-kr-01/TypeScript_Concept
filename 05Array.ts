const arr: number[] = [ 1, 2, 3]

arr.push(5);

const num : string[] = [];

num.push("a");

// another method to specify datatype

const p: Array<number> = [];


type user = {
    name: string;
    isActive : boolean
}

const allUser : user[] = [];

allUser.push({name:"abhi", isActive: false});

const MLModels: number[][] = [ [255, 255, 256 ]]

